# Overview

## bom
Manage external dependencies

## domain
The domain model layer is responsible for maintaining abstract interfaces for entities, value objects, aggregates,
aggregate roots, and resource acquisition.

## service
Domain service layer, used to support the life cycle of domain business

## repository
The data resource layer is responsible for providing the realization of obtaining data resources,
such as the data operation of mysql.

## gateway
The remote resource layer is responsible for providing the realization of obtaining coordinated resources, such as the dubbo service call.

## infrastructure
The infrastructure layer provides low-level components that are irrelevant to the business, such as util, io, mail, and mq.

## application
The application service layer provides business access and is responsible for coordinating business logic.

## client
The presentation layer provides external access to business services
